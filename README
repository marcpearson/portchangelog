NAME

portchangelog


DESCRIPTION

Shell script to retrieve FreeBSD port's changelog from Freshports.org


RUNNING DEPENDENCIES

FreeBSD
Ports in /usr/ports
python 3
beautifulsoup4


HOW TO USE

Copy portchangelog file to /usr/local/bin then call it from command
line like this :

portchangelog port_name
